type ChatBotMessage = {
  message: string;
  conversation_topic: string;
};

type messageContext = {
  pentateuch: string;
  startChapter: string;
  startVerse: string;
  endChapter: string;
  endVerse: string;
};
